# Next.js — copy to .env.local
# npx convex dev sets NEXT_PUBLIC_CONVEX_URL automatically.
# Keep only browser-visible config here.

NEXT_PUBLIC_MATCHER_BASE_URL="http://localhost:4001"
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
NEXT_PUBLIC_MATCHER_MODE="stub"
